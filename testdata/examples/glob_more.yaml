on:
  push:
    branches:
      - '**'
      - '[x]'
    tags:
      - '!*'
      - /v\d+/
      - v[0-
      - v[9-0]
    paths:
      - '!'
      - 'foo\bar'
      -

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo hi
