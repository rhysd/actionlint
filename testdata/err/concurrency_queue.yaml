on: push

concurrency:
  group: test
  queue: invalid

jobs:
  test:
    runs-on: ubuntu-latest
    concurrency:
      group: job-test
      cancel-in-progress: true
      queue: max
    steps:
      - run: echo
