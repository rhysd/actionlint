on:
  push:
    paths: path/to/foo
    paths-ignore: path/to/bar
    branches: foo
    branches-ignore: bar
    tags: v*.*.*
    tags-ignore: dev
  pull_request:
    paths: path/to/foo
    paths-ignore: path/to/bar
    branches: foo
    branches-ignore: bar
  pull_request_target:
    paths: path/to/foo
    paths-ignore: path/to/bar
    branches: foo
    branches-ignore: bar
  workflow_run:
    workflows: foo.yaml
    branches: foo
    branches-ignore: bar

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello
