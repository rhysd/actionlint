name: 'My action'
author: 'rhysd <https://rhysd.github.io>'
description: 'my action'

inputs:
  input1:
    deprecationMessage: minimal message
  input2:
    deprecationMessage: message ending with period.
  input3:
    deprecationMessage: '  message with spaces  '
  input4:
    deprecationMessage: |
      this is first line.
      this is second line.
      this is third line.
  input5:
    deprecationMessage:
  # Usage of this input is not reporeted as error because it is marked as 'required'
  input6:
    required: true
    deprecationMessage: some message
  # Usage of this input is reporeted as error because it is actually not required due to the default value
  input7:
    required: true
    default: some value
    deprecationMessage: some message

runs:
  using: 'node20'
  main: 'index.js'
