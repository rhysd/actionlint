# Issue #263
on:
  workflow_dispatch:
    inputs:
      input1:
        type: string
  workflow_call:
    inputs:
      input2:
        type: string

jobs:
  some-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo '${{ inputs.input1 }}'
      - run: echo '${{ inputs.input2 }}'
