on:
  push:
    branches: main
    branches-ignore: test
    tags: v*.*.*
    tags-ignore: deploy/*
    paths: path/to/foo
  pull_request:
    branches: main
    branches-ignore: test
    paths-ignore: path/to/foo
  pull_request_target:
    branches: main
    branches-ignore: test
    paths-ignore: path/to/foo
  workflow_run:
    workflows: foo.yaml
    branches: main
    branches-ignore: test

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ...
