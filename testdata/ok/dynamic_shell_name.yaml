on: push

defaults:
  run:
    shell: ${{ env.SHELL }}

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: ${{ env.SHELL }}
    steps:
      - run: echo hi
        shell: ${{ env.SHELL }}
